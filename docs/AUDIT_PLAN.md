# План дій: аудит та виправлення hospital-analytics

> Аудит від 2026-05-30. Виправлення виконуються послідовно зверху вниз.
> Статуси: `[ ]` — не зроблено, `[~]` — в роботі, `[x]` — виправлено.

---

## 🔴 Критичні (безпека / витік даних)

### [x] 1. `pages/api/stats.js` — відкрите виконання довільного SQL без авторизації
POST на `/api/stats` бере `req.body.sql` і виконує його напряму через `execute_sql`
з `SUPABASE_SERVICE_KEY` — **без перевірки логіну, без ролі, без `validateReadOnlySql`**.
Будь-хто в інтернеті може прочитати всю БД (імена пацієнтів, діагнози).
**Виправлення:** додати перевірку авторизації (getUserRole), пропускати SQL через
`validateReadOnlySql`, для ролі doctor — застосовувати той самий фільтр що в ask.js,
або взагалі заборонити довільний SQL від клієнта (тільки whitelisted-запити).

### [x] 2. Роль `doctor` бачить дані всієї лікарні через аналітичні VIEW
Фільтр по `doc_name` застосовується тільки якщо в SQL є `FROM lsmd`. Але роутер
для більшості запитів повертає готові VIEW (`v_department_stats`, `department_diagnoses`,
`v_diagnosis_stats`, `v_hospital_summary`), які `lsmd` не містять → фільтр не спрацьовує
→ doctor (Ільніцька) бачить статистику всіх відділень і лікарів.
**Виправлення:** для ролі doctor дозволяти тільки запити з власною фільтрацією по lsmd
(або власні doctor-VIEW), блокувати агрегати по всій лікарні. Розглянути RLS на рівні БД.

### [x] 3. Фронтенд `index.js` не знає про роль
Усі 80+ прикладів (зокрема «Скільки пацієнтів пролікував Грабовський») показуються будь-кому.
`provider = 'groq'` захардкоджено, роль ніде не зчитується.
**Виправлення:** зчитувати роль через app_users, фільтрувати ALL_EXAMPLES по ролі,
ховати приклади «по інших лікарях» для doctor.

---

## 🟠 Поломки (биті SQL-запити)

### [x] 4. Неіснуючі колонки у VIEW (роутер генерує биті SQL)
- `v_peak_by_hour` має `(hour, cases, deaths)`, роутер у 2 місцях бере `admissions, urgent, planned`.
- `v_admissions_by_weekday` — гілка бере `letality_percent` (звірити наявність).
- Автофікс `admissions→cases` у ask.js крихкий і працює лише для `v_peak_by`.
**Виправлення:** привести SQL роутера до реальних колонок VIEW (з аудиту схеми).

### [x] 5. Дублікат блоку `if (t.includes('чернівецьк'))` у query-router.js
Написаний двічі підряд, другий недосяжний. Прибрати.

### [x] 6. Помилки в SYSTEM_PROMPT (ask.js)
- `EXTRACT(HOUR FROM admission_ts::timestamp, 'hour')` — зайвий аргумент.
- `TO_CHAR(DAY FROM admission_date_d::timestamp, 'Day')` — невалідний синтаксис.
- Звірити число рядків icd_10 (промпт: 19,824).
**Виправлення:** виправити приклади SQL у промпті.

---

## 🟡 Невідповідності конфігурації

### [x] 7. Netlify vs Vercel
`netlify.toml` + `@netlify/plugin-nextjs` у репо, але деплой на Vercel (nobodybly.org).
**Виправлення:** видалити netlify.toml, прибрати netlify-плагін з package якщо є.

### [x] 8. Дві різні env-змінні для одного URL
Код використовує і `SUPABASE_URL` (REST у ask/stats/icu), і `NEXT_PUBLIC_SUPABASE_URL` (lib/middleware).
**Виправлення:** уніфікувати на одну змінну, або задокументувати що потрібні обидві.

### [x] 9. `getUserRole` ламає cookie при кількох
`setAll` робить `res.setHeader('Set-Cookie', ...)` у циклі — кожна ітерація перезаписує попередню.
**Виправлення:** збирати масив cookie і ставити одним setHeader.

---

## Дрібниці
- `next.config.js`: `reactStrictMode: false` — не баг, але нерекомендовано.
- `sql-guard`: блокування `set`/`do` як цілих слів — обережно з майбутніми аліасами.

---

## Порядок виконання
1 → 2 → 3 (безпека) → 4 → 5 → 6 (поломки) → 7 → 8 → 9 (конфіг).
Кожне виправлення — окремий коміт. Білд перевіряти перед пушем.

---
## Виявлено під час виправлення (нові пункти)

### [x] 10. SSG prerender падає без env-змінних
`createClient()` / `createBrowserClient` викликається на рівні рендера index.js та login.js.
Під час `next build` (SSG) env-змінних немає → prerender кидає помилку.
На Vercel працює (env задані), але крихко.
**Виправлення:** відкласти створення клієнта (useState/useMemo всередині useEffect),
або позначити сторінки як SSR (getServerSideProps), або обгорнути в try/catch.

### [x] 11. /api/icu закрито авторизацією (зроблено разом з #3)
Був відкритий GET без авторизації — закрито: тепер вимагає логін + блок для doctor.

### [x] 12. query-router.js та sql-guard.js лежать у pages/api/
Next.js трактує їх як API-роути (`/api/query-router`, `/api/sql-guard`), хоча це
допоміжні модулі без handler. Виклик цих URL поверне 500. Не критично (їх ніхто не
викликає ззовні), але правильно — винести в `lib/` і поправити імпорти.

---
## Поглиблений захист БД (RLS-альтернатива, варіант B)

### [x] 13. service_role обходив застосунковий захист + execute_sql була публічною
**Виявлено:** `execute_sql` мала EXECUTE для anon/PUBLIC → будь-хто з браузерним
anon-ключем міг виконати довільний SELECT напряму через Supabase REST, оминаючи
весь застосунковий захист. Плюс простий RLS не спрацював би, бо бекенд ходить
через service_role (обходить RLS за дизайном).

**Зроблено (міграція execute_sql_safe_with_role_guard):**
- Створено `execute_sql_safe(sql_query, p_role, p_doc_name)` — перевірка ролі
  ВСЕРЕДИНІ БД (другий рубіж). Для doctor: запит мусить читати lsmd І містити
  фільтр по власному doc_name, інакше RAISE EXCEPTION. Дублює guard на SELECT-only
  та single-query на рівні БД.
- Відкликано EXECUTE на старій `execute_sql` від PUBLIC/anon/authenticated
  (лишились service_role + postgres).
- Нова функція доступна тільки service_role.
- ask.js / stats.js / icu.js переключено на execute_sql_safe з передачею ролі.

**Перевірено (6 тестів):** admin бачить усе ✓; doctor бачить свої дані ✓;
doctor→загальна VIEW відмова ✓; doctor→чужий лікар відмова ✓;
viewer→загальна пройшов ✓; multi-query "; DROP" відмова ✓.

### [x] 14. Баги, виявлені на проді (скріншот користувача)
**Проблема 1 — "скільки я пролікувала інсультів" → хибна відмова:**
regex `\b(я|мої|...)\b` НЕ працює з кирилицею в JS (\b не визначає межу для не-ASCII),
тому підстановка прізвища для doctor не спрацьовувала → запит ішов у діагнозну VIEW
(відмова) або в LLM. Виправлено: кириличе-сумісні межі `(^|[^а-яіїєґ'])...([^а-яіїєґ']|$)`.

**Проблема 2 — "летальність моїх пацієнтів" → Bad escaped character in JSON:**
коли роутер повертав NULL, запит ішов у LLM, який генерував SQL з невалідним
JSON-екрануванням (\'). JSON.parse падав. Виправлено: санітизація \' та інших
невалідних escape перед парсингом + кількаступеневий fallback.

**Проблема 3 (виявлено при тестуванні) — БД-функція відхиляла ВСІ doctor-запити:**
execute_sql_safe вимагала повного імені ('Ільніцька О. В.'), а застосунок фільтрує
по скороченому прізвищу ('%ільніцьк%') від detectDoctor → 100% відмов на проді.
Виправлено (міграція execute_sql_safe_surname_match_fix): перевірка по стему прізвища.

**Також:** прибрано дублювання doctor-фільтра в routeQueryWithRole.

**Перевірено (5 сценаріїв БД):** doctor скороч.прізвище/повне імʼя — проходять;
чужий лікар/загальна VIEW — відмова; admin — проходить.

### [x] 15. Гілка "мій діагноз" для doctor (покращення)
Додано розпізнавання діагнозу в запитах лікаря (doctorDiagnosis): 21 діагноз з
ICD-префіксами за реальними даними неврології (G55 корінцеві, I63 інсульт, G35 РС,
G40 епілепсія, G93, I61, M51/M54 тощо) + загальні. Для "мої інсульти" повертається
СПИСОК власних випадків (дата надходження, МКХ, відділення, вислід, ліжкодень),
LIMIT 200, з періодом якщо вказано. Перевірено через execute_sql_safe на реальних
даних Ільніцької (I63.3, Центр невідкладної неврології). Додано приклади у фронтенд.

### [x] 16. Нова модель доступу + достовірність + ПІБ власних пацієнтів
**Вимоги:** без помилок; достовірні відповіді; лікар бачить власних пацієнтів + загальні агрегати.

**Модель доступу doctor (БД execute_sql_safe + ask.js, два рубежі):**
- агрегатні VIEW (без ПІБ, перевірено 20 VIEW) — дозволені (для порівняння);
- lsmd — лише з фільтром по власному прізвищу;
- patients_best/patients/encounters — заборонені (дані всіх пацієнтів);
- JOIN для doctor — заборонений (закрито обхід через декартів `JOIN ... ON true`,
  який давав ПІБ усіх пацієнтів лікарні — критичний витік, виявлено й виправлено).

**ПІБ власних пацієнтів:** додано patient_name у список випадків по діагнозу
(перевірено: Кулик А.О., Лисенко Н.Д. — реальні пацієнти Ільніцької).

**Доступ до агрегатів повернено:** /api/stats та /api/icu більше не блокують doctor
(агреговані дані без ПІБ); фронтенд показує doctor власні + загальні приклади та цифри.

**Достовірність — прогнано всі 39 унікальних SQL роутера через БД:**
0 помилок. Покрито: doctor-статистику, списки з ПІБ, агрегати, райони, діагнозні JOIN.
Перевірено окремо підозрілі колонки (operations, urgent_deaths, position,
letality_percent, readmit_*) — усі існують.

**Тести ізоляції (БД):** doctor→агрегат OK; doctor→свій lsmd OK; doctor→lsmd без
фільтра ВІДМОВА; doctor→чужий лікар ВІДМОВА; doctor→patients_best ВІДМОВА;
doctor→JOIN patients_best ВІДМОВА; admin→все OK.

### [x] 17. Фінальна перевірка сценаріїв лікаря + закрито UNION/підзапит-обхід
Відтворено всі 18 сценаріїв лікаря через задеплоєну логіку роутера + execute_sql_safe:
- статистика (пролікував/нічні/вихідні/летальність/операції) — OK;
- списки власних пацієнтів з ПІБ (інсульт/епілепсія) — OK;
- спеціалізація — OK;
- загальні агрегати (топ діагнозів/летальність відділень/реанімація) — OK (дозволені);
- спроба чужого лікаря (Грабовський) — BLOCK.

**Виявлено й закрито 2 вектори обходу (критично):**
- UNION-обхід: `WHERE doc_name ILIKE '%свій%' UNION SELECT patient_name FROM lsmd`
  пропускався (фільтр у 1-й частині, 2-га читала всіх). Заборонено UNION/EXCEPT/INTERSECT.
- Підзапит-обхід: множинне звернення до lsmd. Заборонено >1 входження lsmd для лікаря.

**Фінальні тести ізоляції — усі 7 правильні:** L1 всі пацієнти BLOCK, L4 JOIN BLOCK,
L5 підзапит BLOCK, L6 UNION BLOCK; свій список/статистика/агрегат — ПРОЙШОВ.
Заборони продубльовано в ask.js (зрозумілі повідомлення замість БД-помилок).

### [x] 18. Доктор: запит про діагноз без "я" давав загальну статистику
На проді "скільки пролікувано інсультів" (без "я") для лікаря йшло в v_diagnosis_stats
(загальна статистика діагнозу по лікарні) замість власних випадків — неінтуїтивно.
Виправлено: для ролі doctor запит ЗА ЗАМОВЧУВАННЯМ стосується власних даних
(підставляється прізвище → особиста гілка lsmd), КРІМ явно загальних запитів
(по лікарні/відділеннях/районах/реанімація/топ/демографія тощо — лишаються агрегатами
для порівняння). Перевірено: інсульти/епілепсія/операції/летальність → власні;
топ діагнозів/по відділеннях/реанімація/загальна → агрегати.

### [x] 19. Метрика "нічні поступлення" недостовірна (дані)
admission_ts відображає час реєстрації в системі (пік 9-12 год), не фактичного
надходження. "Нічних" виходить 15/625 (2.4%) — формально вірно за SQL, але вводить
в оману. Потребує рішення: прибрати метрику для лікаря / перейменувати на "за часом
реєстрації" / лишити. ОЧІКУЄ відповіді користувача.

**Рішення #19 (виконано):** реалізовано логіку чергувань на запит користувача:
- доба чергування = 09:00→09:00 (надходження до 09:00 належить попередній добі);
- нічні поступлення = 22:00–07:00 (було 22:00–06:00);
- гілка "нічні": показує нічних_поступлень + діб_чергування_з_нічними;
- нова гілка "доби чергування": COUNT DISTINCT діб 09:00→09:00 (Ільніцька 2024: 204 доби, 625 поступлень; 15 нічних на 14 діб);
- назва чесно позначена "за часом у системі" (admission_ts = час реєстрації, пік 9-12).

### [x] 20. Приклади для лікаря: прибрано непрофільні (хірургія/урологія/тощо)
Лікар-невролог бачив у прикладах хірургію, апендицити, простату, урологію — бо для
doctor показувались DOCTOR_EXAMPLES + увесь ALL_EXAMPLES. Створено окремий
DOCTOR_GENERAL_EXAMPLES (лише агрегати, доречні неврологу: загальна статистика,
летальність по відділеннях/діагнозах, реанімація, піки, динаміка, вікові групи,
реадмісія). Власні приклади розширено неврологічними (ТІА, епілептичні напади),
прибрано "операції у мене" (невролог не оперує).
Виправлено баг розпізнавання: "транзиторна ішемічна атака" ловилась як ішемічний
інсульт (ключ ' ішемічн' був надто широкий) — ТІА(G45) тепер перевіряється першою,
ключ інсульту звужено.

### [x] 21. Групування показників по клінічних напрямках (терапія/хірургія)
ВИЯВЛЕНО: класифікація вже повністю існує в БД (з попередніх сесій):
- clinical_blocks: 1=хірургічний, 2=терапевтичний, 3=інтенсивна_терапія, 4=параклінічний;
- departments.block_id — прив'язка кожного відділення;
- analytics_block_summary — готова зведена аналітика по напрямках × роках (2020-2026, заповнена).
На рівні БД будувати нічого не треба. Бракувало лише доступу через роутер.
ДОДАНО: гілка роутера "по напрямках" (читає analytics_block_summary JOIN clinical_blocks),
тригери: "напрям", "по блоках", "блок+статистика", з опційним роком. Перевірено через
execute_sql_safe (2024: хірургічний 12141/68.88% операцій, терапевтичний 8370/2.11%,
інтенсивна 426/30.52% летальність). Агрегат без ПІБ — доступний і лікарю. Додано приклади.

### [x] 22. Напрямки без року — за весь період (узгодження із загальною статистикою)
Гілка "по напрямках" без вказаного року брала лише останній рік (2026) → 6642 випадки,
що не сходилось із загальною статистикою лікарні (110206 за весь період). Виправлено:
без року — SUM по всіх роках (хірург. 67154, терап. 39781, інтенсив. 2581 = 109516 ≈ 110206);
хірург_акт рахується як зважене SUM(операцій)/SUM(випадків). З роком — конкретний рік.

### [x] 23. Гілка "відділення напрямку"
Додано: "скільки/які відділення хірургічного/терапевтичного напрямку" → список відділень
з кількістю лікарів і персоналу (departments JOIN clinical_blocks). Перевірено: хірургічний
напрямок = 7 відділень (Хірургічні №1/№2, Травматологічні дорослі/діти, Урологічне,
Нейрохірургічне, Опікове). Не перехоплює загальну гілку "показники по напрямках".

### [x] 24. Гілка "лікарі відділення"
"покажи лікарів з опікового відділення" давало статистику відділення замість списку
лікарів (не було гілки). Додано: тригери "лікар/персонал/хто працює/завідувач" +
назва відділення → список лікарів (emp_name, спеціалізація, посада), завідувач першим.
Читає empl (не lsmd) — дозволено й лікарю (колеги, не персональні дані пацієнтів).
Перевірено: опікове = 6 (завідувач Семенюк + 5 ординаторів-комбустіологів).

### [x] 25. Синонім "доктор" = "лікар"
"покажи докторів опікового" не розпізнавалось (давало статистику / NULL), бо тригери
містили лише "лікар". Додано "доктор" у гілки: лікарі відділення (р.166) та лікарі за
спеціалізацією (р.220). Перевірено: доктори/лікарі відділення, доктори-хірурги — усе ловиться.

### [x] 26. Номер відділення №1/№2 ігнорувався; хірургія плуталась
"лікарі з хірургії №1" видавало лікарів обох хірургій + нейрохірургії (фільтр ILIKE
'%хірургічн%' ловив 3 відділення, номер ігнорувався). Виправлено:
- лікарі/діагнози/категорії відділення тепер фільтрують по ТОЧНІЙ назві (department=),
  з урахуванням номера (№1/№2) для хірургії та терапії, дорослі/діти для травматології;
- ключ deptMap 'хірургічн'→'хірург' (ловить і "хірургії"); нейрохірург перевіряється раніше;
- гілка "відділення напрямку" тепер лише за явним словом напрям/блок (не перехоплює
  "хірургічного відділення");
- "доктор" як синонім (з #25) збережено.
Перевірено: №1/№2/нейрохірургія/терапевт №2/травма діти-дорослі — усе точно;
БД: хірургія №1 = 34 лікарі (завідувач Філіпець + ординатори), без №2.

### [x] 27. "Всіх хірургів" (спеціальність) vs "хірургія №1" (відділення)
"покажи всіх хірургів" давало статистику Хірургічного відділення №1 замість списку
всіх лікарів за фахом хірург. Виправлено розмежування спеціальність/відділення:
- deptMap пропускає спец-ключі (хірург/терапевт/уролог/травматол/гастро/гематолог/нейрохірург),
  якщо в запиті НЕМАЄ слова "відділ"/"центр"/номера → запит про фах, не відділення;
- гілка спеціальностей: точний матч specialization='хірург' (не ILIKE, щоб не захопити
  нейрохірурга), нейрохірург перевіряється раніше, додано тригер "покажи";
- результат за фахом сортується по відділеннях.
Перевірено: всі хірурги=65 (2 відділення, без нейрохірургів); нейрохірурги окремо;
"хірургія №1" → відділення; терапевти за фахом vs терапевтичне відділення №1 — різне.

### [x] 28. Спеціальності (урологи/комбустіологи/травматологи) + лікарі напрямку
ПЕРЕВІРЕНО всі спеціальності в БД. Знайдено: травматологи мають 2 значення
('травматолог' 54 + 'травматолог дитячий' 14) — точний матч давав лише 54.
Виправлено: травматолог → LIKE 'травматолог%' (тепер 68). Урологи (23), комбустіологи (6) — OK.
ДОДАНО розрізнення трьох рівнів (на прохання користувача):
- "скільки хірургів" = фах (65, тільки spec='хірург');
- "скільки лікарів хірургічного напрямку" = весь блок (173: хірурги+нейрохірурги+урологи+
  травматологи+комбустіологи+онкологи через clinical_blocks);
- "скільки відділень хірургічного напрямку" = 7 відділень.
Перевірено всі три + терапевтичний напрямок (60) — не плутаються.

### [x] 29. Картка пацієнта за ПІБ (тільки admin)
Структура пацієнтів: patients_best (72293 картки, patient_id унікальний, з full_name)
↔ lsmd (110206 госпіталізацій, 67856 унік. пацієнтів) через patient_id. Цілісність 100%.
Пошук по full_name (68238 унік., ~4000 тезок — для них кілька рядків з датою народження).
ДОДАНО гілку "картка/історія пацієнта <ПІБ>" — ТІЛЬКИ role=admin:
повна картка (ПІБ, вік, стать, телефон, адреса, район) + усі госпіталізації
(дата, МКХ, відділення, лікар, вислід, ліжкодень). ПІБ витягується regex по словах
з великої літери. Захист 2 рівні: роутер не дає гілку doctor (NULL);
execute_sql_safe блокує patients_best для doctor (перевірено — відмова).

### [x] 30. Помилка "column date_of_birth does not exist"
Причина: у SYSTEM_PROMPT (ask.js) таблиця patients_best була описана з НЕІСНУЮЧОЮ
колонкою date_of_birth → LLM генерував SQL із нею. Виправлено:
- опис patients_best у промпті замінено на реальні колонки (full_name, patient_name,
  age, birthday, gender, phone_num, address, district, region, locality);
- додано явне правило: використовувати тільки перелічені колонки, не вигадувати
  англійських назв; дата народження = birthday, вік = age.
Звірено ВСІ таблиці/VIEW промпту з реальною схемою БД — інших розбіжностей немає
(peak-view, аналітичні VIEW, empl, icd_10 — усі колонки збігаються).

### [x] 31. Картка пацієнта: двоступеневий пошук (прізвище → вибір → картка)
На прохання: коли введено лише прізвище — показати список людей для вибору.
Реалізовано: 1 слово = список (full_name, вік, стать, район, нас.пункт, к-сть госпіталізацій,
ILIKE 'Прізвище %'); 2+ слова = повна картка з історією. На популярні прізвища до 249
різних людей, тож вибір обов'язковий. Гілку картки ПЕРЕНЕСЕНО перед детектором лікаря
(бо прізвище пацієнта може збігатися з лікарем, напр. Ткачук — тепер пацієнт має пріоритет
при явному слові "пацієнт/картка"). Захист doctor збережено (NULL/статистика, не patients_best).

### [x] 32. Розрізнення "лікар Ткачук" vs "пацієнт Ткачук"
Питання користувача: чи при пошуку лікаря Ткачука не лізтимуть пацієнти.
Перевірено: "лікар/статистика/скільки пролікував Ткачук" → лікар (OK). Але знайдено
колізію: "Ткачук скільки пацієнтів" (= статистика лікаря) помилково давало список
пацієнтів через слово "пацієнтів". Виправлено: гілка картки спрацьовує лише при
явному намірі (картка/історія хвороби, або "пацієнт <ПІБ>") І ЗА ВІДСУТНОСТІ слів
лікарської статистики (скільки/пролікував/лікар/летальн/операц/нічн/чергуван/вихідн/діб).
Також фікс кириличного \b у regex (не працює) → (\s+|$). Усі 6 контрольних — OK.

### [x] 33. Дизайн-система «Медична» — інтеграція (Спосіб Б: dashboard)
Поєднано робочий hospital-analytics з дизайн-системою medychna (з Mac):
- globals.css: повна токен-система бренду (paper/ink/teal-ink/ember + семантика,
  Instrument Serif + IBM Plex Sans/Mono, radii/shadow) + зворотні аліаси старих змінних;
- index.js переписано як dashboard: Sidebar (навігація + профіль ролі + лічильники лікарні),
  TopBar (різний для admin/doctor), контент-зона, поле вводу;
- ResultView у стилі бренду: велике число (Instrument Serif), картки-показники,
  таблиця з hairline rules, моноширинні tabular-nums, Badge для статусів/летальності;
- примітиви Icon/Avatar/Badge/Card перенесено інлайн (Next.js не вантажить .jsx через babel);
- укр. формат чисел (десятковий розділювач — кома, тисячі — тонкий пробіл).
БЕКЕНД НЕ ЗМІНЕНО: роутер, /api/ask|me|stats|icu, захист ролей, гілки — усе як було.
Контракт API {explanation, rows} збережено. Старий index збережено як index.old.backup.js.

### [x] 34. Реальне ім'я й посада залогіненого користувача в сайдбарі
/api/me розширено: через app_users.empl_name_id → empl.name_id дістаються
full_name, position, specialization. Sidebar і TopBar показують реальні дані
(doctor: "Ільніцька Олеся Вікторівна · ординатор · невропатолог"), з фолбеком
для admin (без empl_name_id → "Адміністратор · Повний доступ"). Зашиті імена прибрано.

### [x] 35. Дизайн «ЛСМД Дашборд» — Огляд на ЖИВИХ даних (гілка design-medychna)
Узято АКТУАЛЬНИЙ дизайн (експорт Claude Design з iCloud, ui_kits/dashboard):
монохром (тепле тло #f5f4f0, текст #1a1917, IBM Plex Sans/Mono) + червоний акцент #c0392b.
- globals.css переписано на правильні токени (замість хибних Instrument Serif/teal);
- index.js → оболонка дашборду: Sidebar (+ ЛСМД, навігація, профіль ролі), AI Асистент
  (робочий чат збережено) + сторінка ОГЛЯД на ЖИВИХ даних;
- /api/stats whitelist розширено: ovKpi/ovHours/ovStatus/ovIcd (через execute_sql_safe,
  без довільного SQL від клієнта, авторизація обов'язкова);
- Огляд показує: 4 KPI (з v_hospital_summary), барчарт по годинах (v_peak_by_hour,
  пік червоний), тип госпіталізації, топ-7 розділів МКХ, статуси виписки — усе з БД наживо.
Перевірено: усі ключі віддають правильні дані (110206, 2.06%, пік 10:00, K/S/I...).
Решта розділів (Відділення/Лікарі/Діагнози/Пацієнти/Географія/Піки/Нічні) — наступні етапи.
main НЕ чіпали. Бекенд (роутер/ask/захист) без змін.

### [ ] 36. ДИЗАЙН: точна копія сайту з Claude Design (IN PROGRESS — продовжити в новому чаті)
МЕТА (за словами користувача): зробити ІДЕНТИЧНИЙ сайт як у Claude Design (усі 13
розділів + картки), залити на nobodybly.org ЗІ ЗАШИТИМИ даними as-is. Живі дані — ПОТІМ.

ДЖЕРЕЛО ДИЗАЙНУ (актуальне, не плутати зі старим medychna в Downloads):
/Users/julienblyndu/Library/Mobile Documents/com~apple~CloudDocs/ProCreate/ЛСМД Design System/
  ui_kits/dashboard/ — index.html, App.jsx, Dashboard.jsx (~879 рядків, ПОВНИЙ),
  Marketing.jsx, dashboard.css, colors_and_type.css.
Доступ до iCloud ТІЛЬКИ через Filesystem MCP (bash контейнера НЕ бачить iCloud).
Інструмент копіювання: Filesystem:copy_file_user_to_claude (path) → кладе на контейнер Клода.

13 розділів навігації (з Dashboard.jsx): Аналітика[Загальна/Огляд, Відділення, Діагнози МКХ,
Лікарі, Пацієнти(=демографія, БЕЗ ПІБ — безпечно), Географія(Leaflet-карта нп), Піки,
Нічні зміни, Приймальне, Операції] + Інструменти[AI Асистент(=чат), Звіти, Налаштування].
Дизайн: монохром #f5f4f0/#1a1917, IBM Plex Sans/Mono, акцент червоний #c0392b, бренд-знак "+".
Стек дизайну: статичний HTML + React18 UMD + Babel standalone (CDN), JSX-файли, дані ЗАШИТІ в JSX.

ПЛАН (узгоджено): подати ці САМІ файли as-is через Next.js public/, щоб було 1-в-1.
Поточний стан гілки design-medychna:
- public/dashboard/ має 6 файлів, АЛЕ Dashboard.jsx ОБРІЗАНИЙ (11КБ, лише DashSidebar+OverviewPage,
  решти 11 сторінок НЕМАЄ — тому "зовсім не те"). ПОТРІБНО перекопіювати ПОВНИЙ з iCloud.
- pages/index.js на гілці — переписаний дашборд (Огляд на живих даних + Асистент), лише 2 сторінки.

НАСТУПНІ КРОКИ (новий чат):
1. Filesystem:copy_file_user_to_claude для всіх 6 файлів з iCloud ui_kits/dashboard → /home/claude.
2. Скопіювати їх у public/dashboard/ (повні версії).
3. Варіант подачі: або pages що віддає public/dashboard/index.html, або iframe, або
   переписати index.html під Next public-флоу (CDN React+Babel працює в браузері as-is).
   ВАЖЛИВО: babel-standalone у браузері — ок для статики; перевірити CSP/завантаження JSX з /public.
4. ЗАШИТІ дані лишити як є (демо-числа з дизайну). Узгоджено: спершу копія, потім живі дані.
5. Залити на nobodybly.org = вмерджити design-medychna в main + переконатися що Vercel
   зробив НОВИЙ production deploy (минулого разу force-push НЕ тригернув передеплой — прод
   застряг на старому білді #34; preview гілки був ОК).

ВАЖЛИВО ПРО VERCEL: team_wp8qV3ziHw0eKgh29OchDQ2F, project prj_KavwNImB07wBrDbWxuTr7U13Emco.
Після зміни main перевіряти Vercel:list_deployments що production target оновився; force-push
може не тригерити redeploy — можливо тригернути новим комітом або через Vercel deploy.

БЕЗПЕКА: сторінка "Пацієнти" в дизайні = демографія (агрегати вік/стать), БЕЗ реальних ПІБ —
заливати безпечно. Реальні картки пацієнтів за ПІБ (фіча #29/#31) — ОКРЕМО, лише admin, не в цьому статичному дизайні.
