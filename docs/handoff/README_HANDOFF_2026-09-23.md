# Handoff 2026-09-23 — ролі, навігація, паролі персоналу

Гілка `qwerty-project-connection-e6mjy9`, робоча копія `~/hospital-analytics-qwerty` (порт :3000). Лікарня — ЛШМД (`43342788`), Supabase `ubjnztanehqlsrqphdqy`.

## Стан репозиторію

Запушено до `baa0106`. Робоче дерево чисте.

| Коміт | Що |
|---|---|
| `a86e02c` | census-bar: число днів узгоджено з кількістю рисочок при обрізанні (60 сегментів — стеля) |
| `490766a` + `999f61d` | `me.js`: `role` — пріоритет `lpz_role` над `app_users.role`; для 9 людей з однаковим email на 2 рядках `lpz_empl` (лікар+завідувач того самого відділення) — пріоритет старшої ролі (`chief`>`deputy`>`head`>`doctor`>`nurse`), бо `.maybeSingle()` при 2+ збігах мовчки ламав `full_name`/`lpz_role` |
| `4a40acb` | `quality-notes.js`: у компактному списку "Зауваження до записів" — короткий опис зауважень поруч із цифрою, клік підсвічує лікаря в ординаторській |
| `baa0106` | Навігація: редірект на `/layout.html` тепер несе `?next=`, повертає туди, звідки прийшли (в т.ч. у фінрежим), а не завжди на `/entry.html`; "Контроль записів →"/"← КАБІНЕТ" теж через `next` |

## Що зроблено, але НЕ в git (база/інфраструктура)

- **Синхронізація канону (ранок):** `lpz_hospitalizations` закрито лаг 08.09→22.09 (+905 записів).
- **Вечірній прогін якості:** `runQualityDaily` — 60 нових зауважень, 18 виправлено.
- **Видалено 110 фейкових госпіталізацій** — артефакт масового закриття 19.08.2026 (усі мали `discharge_date='2026-08-19'`, `admission_date<'2026-06-01'`, різні пацієнти): госпіталізації + діагнози + рядки `lpz_case_quality_snapshot`. Перевірено: "хвостів" квітня/травня більше нема, залишок дат виписки розкиданий природно.
- **Паролі персоналу ЛШМД:** усі 763 унікальних email з `lpz_empl` тепер мають Supabase Auth акаунт із паролем `Lsmd2026` (208 існуючих скинуто, 554 створено заново через Admin API). Тестові скрипти видалені одразу після використання (не в git).

## Відкочено (пробували, не лишилось нічого)

Все нижче застосовувалось і потім **повністю відкочено** (`git checkout -- .` до `baa0106`) на прохання користувача — у робочому дереві й проді сліду нема:
- Розмиті краї `.layout-field` (light-режим) через `::before`+`filter:blur`+`overflow:hidden`.
- Брендинг ЛШМД на сторінці входу (`layout.html` без `?org=` — кеш `lpzLastOrg`).
- Кольори смуги "Для працівників" (`--band-c1/--band-c2`) під лікарню.
- "Glow"-стиль фінрежиму (темно-синій фон, ціан, text-glow — за зразком `hospital-analytics/pages/glow.js`).

Якщо колись повертатись до будь-чого з цього — не з нуля, а звірити з описом вище й з `hospital-analytics/pages/glow.js` (старий проєкт) для glow-частини.

## Пастки/нотатки

- `.maybeSingle()` на `lpz_empl` по email ламається мовчки при 2+ збігах — перевіряти перед новими lookup-запитами по email.
- Видаляючи "підозрілі" госпіталізації — завжди перевіряти конкретні приклади вручну (кілька записів з тим самим шаблоном виявились справжніми, не артефактом).
- Тестові акаунти персоналу: логін = email з `lpz_empl`, пароль `Lsmd2026` — для перевірки кабінетів лікаря/завідувача/головного.
